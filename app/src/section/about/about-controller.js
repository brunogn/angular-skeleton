'use strict';

/**
 * Defines the controller of the "About" section.
 *
 * @param {RootScope} $scope reference to AngularJS's scope
 */
angular
    .module('myApp.about.controller', [])
    .controller('AboutController', ['$scope', function($scope) {
    }]);
