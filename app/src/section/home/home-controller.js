'use strict';

/**
 * Defines the controller of the "Home" section.
 *
 * @param {RootScope} $scope reference to AngularJS's scope
 */
angular
    .module('myApp.home.controller', [])
    .controller('HomeController', ['$scope', function($scope) {
    }]);
